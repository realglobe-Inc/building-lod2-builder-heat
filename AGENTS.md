---
適用: 常時
---

# building-lod2-builder-heat: AI向け開発ガイドライン（プロジェクト固有）

この文書は、本リポジトリに貢献するAIアシスタント向けの実行時指示です。明示的なユーザー要求を最優先とし、この文書に記載のない仕様は既存コードとテストから判断してください。バージョンやツール設定は `pyproject.toml`、実行時の振る舞いはコードとテストを真実のソースとします。

## 1) 変更の基本方針

- 変更は要求を満たす最小限の範囲に限る。目的に無関係なリファクタや命名変更を混ぜない。
- 明示的に変更対象として指定された仕様は、要求がない限り旧仕様との後方互換性を維持しなくてよい。変更対象でない外部仕様（CLI名、引数の意味、出力ファイル名、戻り値、終了コードなど）は維持する。
- 公開APIとして文書化された関数・クラスや外部から利用されるCLIを破壊的に変更する場合は、要求と影響範囲を確認する。内部モジュール間の参照を変更する場合は参照元も同時に更新する。
- 既存のパターン、命名、ファイル配置に合わせる。`commands/*/main.py` は Typer のCLI定義と対象走査・入出力の統括を担い、対象単位の大きな処理は、既存構成に合わせて `main_unit.py` へ分離する。
- 新規のファイル名は `src/building_lod2_builder_heat/common/file_names.py`、パラメータキーは `src/building_lod2_builder_heat/common/parameter_keys.py` に追加する。パラメータの読み書きには `common/parameter.py` のヘルパを使用する。
- 1ファイル1000行以内を目安とするが、行数だけを理由に機械的に分割しない。責務、依存方向、凝集度、テスト容易性を考慮し、循環参照や過大な引数リストを生まない分割にする。
- 新規依存を追加する前に、標準ライブラリと導入済み依存で実現できないか確認する。追加が必要な場合は、用途に応じて通常依存と開発依存を分け、`pyproject.toml` と `poetry.lock` を整合させる。
- 質問への回答や調査だけを求められた場合は、コードを永続的に編集しない。動作確認のために一時的に変更した場合は、最終的に元の状態へ戻す。

## 2) コードスタイルとドキュメンテーション

- Python バージョンは `pyproject.toml` の `[project].requires-python` を正とし、Python 3.14 を前提に記述する。`# -*- coding:utf-8 -*-` は追加しない。
- 新規または変更する公開関数・メソッドには型ヒントを付ける。`Union` ではなく `|`、`Optional` ではなく `| None` を使う。
- Python 3.14 では annotation が既定で遅延評価されるため、新規ファイルに `from __future__ import annotations` を追加しない。既存ファイルを実質的に変更する場合は、annotation の文字列表現に依存していないことを確認したうえで削除する。この削除だけを目的とした一括変更は行わない。
- フォーマットとLintは `ruff` に従う。相対インポートは禁止する（`ban-relative-imports = "all"`）。インポートは原則としてファイル先頭に置くが、遅延読み込み、任意依存、循環参照の回避に必要なローカルインポートは認める。
- 新規または変更する関数・クラス・モジュールの docstring は reStructuredText 形式にする。依頼と無関係な既存 docstring の形式変更は行わない。Google/NumPy 形式は新たに導入しない。

  ```python
  def example(a: int, b: int) -> int:
      """
      1行要約。

      詳細な説明が必要なら段落で追記する。

      :param a: 入力 a の説明。
      :param b: 入力 b の説明。
      :returns: 戻り値の説明。
      :raises ValueError: エラーとなる条件の説明。
      """
      ...
  ```

- 属性やクラスの docstring では `:ivar name:`、`:cvar name:`、`:raises:` などのフィールドを必要に応じて使う。
- コメントは、非自明な制約、判断理由、アルゴリズムの前提を残す場合に追加する。コードの動作をそのまま言い換えるコメントは追加しない。
- ログや `print` の追加は必要最小限にし、既存テストが依存する標準出力を意図せず変更しない。

## 3) テストと検証

- テストフレームワークは pytest。変更範囲に対応する軽量ユニットテストを優先し、必要に応じて統合テストへ広げる。
- コードを変更した場合は関連テストを必ず実行する。ドキュメントだけの変更で、実行時の振る舞いに影響しない場合は pytest を必須としない。
- CLI の成功系テストでは `typer.testing.CliRunner` を使用し、`result.exit_code == 0` を確認する。エラー系では、想定した終了コード、出力JSON、エラー内容を確認する。
- CLI 成果物の期待値は `test_data/answers/` に配置し、既存のJSON比較方法に従う。期待値は、要求による意図した振る舞いの変更を反映する場合に限って更新し、テストを通すためだけに更新しない。
- `integration` マーカーのテストは HEAT、PyTorch、学習済みチェックポイントを必要とし、チェックポイントのダウンロードでネットワークを利用する場合がある。統合テストは変更に必要で実行環境が整っている場合に実行し、実行できない場合は理由を報告する。
- 変更と無関係と考えられるテスト失敗は、可能な限り変更前の状態でも再現することを確認する。修正対象外とする場合は、実行コマンドと失敗内容を報告する。
- テストや公開インターフェースを不必要に書き換えない。テストを削除、スキップ、弱体化して失敗を隠さない。

## 4) プロジェクト構造

- ソースは `src/building_lod2_builder_heat/` に配置する。
  - `commands/*/main.py`: Typer CLI エントリ。対象走査、モデル呼び出し、エラー処理を統括する。
  - `commands/*/main_unit.py`: 対象単位の処理や、CLIから再利用する処理を配置する。
  - `commands/*/dataset.py`: 推論入力の探索、読み込み、前処理を配置する。
  - `common/*`: ファイル名、パラメータI/O、ログ設定などの共通ユーティリティ。
- `pyproject.toml` の `[project.scripts]` は各 Typer アプリの `app` にマップされる。現在のCLIは `extract-roofline` である。
- 入力対象の走査、スキップ条件、出力配置、バックアップ、対象単位のエラー処理は、明示された変更対象を除き既存実装に合わせる。

## 5) 成果物JSONの方針

- `extract_roofline.json` は屋根線抽出の主要成果物である。`ROOFLINE_CORNERS` と `ROOFLINE_EDGES` は成果データ、`SOURCE_RGB` は成果物と組み合わせて利用するメタデータとして扱う。
- 対象単位の処理に失敗した場合は、既存実装に合わせて `ERROR` と `TRACEBACK` を記録する。エラー情報を追加するために、成功したように見える空の成果データを作らない。
- 出力スキーマを変更する要求がある場合は、再現性や後続処理に必要な入力情報と成果データを優先する。並列数、GPU/CPU選択、ログ設定など、成果物の意味を変えない実行都合の値は記録しない。診断値は原則としてログへ出す。
- APIキー、認証情報、トークン、個人情報などの機密情報は、成果物、ログ、テストデータへ記録しない。
- 新規キーは `src/building_lod2_builder_heat/common/parameter_keys.py` に追加し、`common/parameter.py` のヘルパで読み書きする。

## 6) 実行コマンド

- Poetry 環境がある前提で実行する。
- 変更した Python ファイルのフォーマット: `poetry run ruff format <changed files>`
- 変更した Python ファイルのLint修正: `poetry run ruff check --fix <changed files>`
- 全体Lint（自動修正なし）: `poetry run ruff check .`
- 関連テスト: `poetry run pytest -q <test paths>`
- 統合テスト除外: `poetry run pytest -q -m "not integration"`
- 統合テストのみ: `poetry run pytest -q -m integration`
- 全テスト: `poetry run pytest -q`
- CLIヘルプ確認: `poetry run extract-roofline --help`

## 7) 禁止事項

- 目的に無関係な大規模リファクタや命名一括変更。
- テストを削除、無効化、弱体化して失敗を隠す行為。
- テストを通すためだけの期待値更新。
- 相対インポートの導入、未使用依存の追加。
- 機密情報をコード、成果物、ログ、テストデータへ記録する行為。
- 明示的な要求なしに、学習済みチェックポイントや生成された出力ファイルをリポジトリへ追加する行為。
